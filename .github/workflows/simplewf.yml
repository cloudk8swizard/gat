name: Shell command

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: eccho a string
        run: eccho "Hello Git World"
      - name: multi-line script
        run: |
          node -v
          npm -v
          hostname